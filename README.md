# Matchmaking

A minimal onboarding app for collecting consented social profile information (Instagram, LinkedIn, and optional online presence details) to support matchmaking setup.

## Run locally

Because this is a static app, you can run it with any static file server.

```bash
python3 -m http.server 4173
```

Then open <http://localhost:4173>.
